#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include <conio.h>

double round(double f);

int main(void)
{
	int b;
	double a;
	printf("실수를 입력하시오:");
	scanf_s("%lf", &a);
	b = round(a);
	printf("반올림한 값은 %d입니다.\n", b);
	return 0;
}

double round(double f)
{
	return (int)(f + 0.5);
}

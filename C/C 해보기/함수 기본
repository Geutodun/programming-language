#include <stdio.h>

int get()
{
	int a;
	printf("정수를 입력하세요 : ");
	scanf_s("%d", &a);
	return a;
}

int sum(int a, int b)
{
	return a + b;
}

int main()
{
	int x = get();
	int y = get();
	printf("두 수의 합은 %d입니다.", sum(x, y));

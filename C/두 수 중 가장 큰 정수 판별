#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>


int getMax(x, y)
{
	if (x > y)
		return (x);
	else
		return (y);

	return 0;
}
int main(void)
{
	int x, y, m;
	printf("정수 입력:");
	scanf("%d %d", &x, &y);
	 

	printf("가장 큰수는 %d입니다.", getMax(x, y));

	return 0;
}

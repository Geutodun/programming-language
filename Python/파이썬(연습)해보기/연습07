print("저는 %d만원인 레이저 케이스를 살거예요" % 30)  #%d는 정수
print("저는 %s브랜드를 좋아해요" % "레이저")  #%s는 문자열등 다 가능
print("레이저는 영어로 %c로 시작해요" % "R")  #%c는 한 단어만 받음


print("%s의 주 컬러는 %s입니다." % ("레이저", "녹색")) # 가로 안에 있는 순서대로 입력


print("저는 {}를 좋아합니다".format("레이저")) #%s랑 같은 역할
print("저는 {0}에서 나오는 {2}와 {1}를 쓰고 있어요".format("레이저", "키보드", "마우스"))
#처음부터 순서를 기입하여 나오는 순서를 바꿀 수 있다

R = "레이저"
r = 20
print("저는 {}만원짜리 {}케이스를 샀어요".format(r, R)) #가로안의 순서대로 나옴
print(f"나는 {R}브랜드에 {r}만원을 질렀어요") #파이썬 3.6.이상부터
